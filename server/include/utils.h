// include/utils.h
#ifndef UTILS_H
#define UTILS_H

// Объявления функций
void get_input_string(char *buffer, int size);  // Получение строки с ввода
void safe_str_copy(char *dest, const char *src, int dest_size);  // Безопасное копирование строки

#endif
